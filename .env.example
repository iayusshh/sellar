# Database (Postgres for local + Vercel)
# Example (Neon/Supabase/Railway):
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB?sslmode=require"
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB?sslmode=require"

# Vercel Postgres integration (alternative)
# If you add the Vercel Postgres integration, you can set DATABASE_URL to the
# provided POSTGRES_PRISMA_URL, or just rely on the app’s fallback.
# POSTGRES_PRISMA_URL="..."
# POSTGRES_URL="..."

# Auth.js / NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="change-me-in-dev"

# Credentials auth (used for bcrypt password hashing)
# No extra env vars needed.

# Stripe (demo/prototype only)
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=""

# Platform commission (0.15 = 15%)
PLATFORM_COMMISSION_RATE="0.18"

# Email (optional)
# If RESEND_API_KEY is empty, emails will be logged to the server console.
RESEND_API_KEY=""
EMAIL_FROM="Sellar <no-reply@sellar.local>"

# Uploads (dev local)
NEXT_PUBLIC_APP_URL="http://localhost:3000"
